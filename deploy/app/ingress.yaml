apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cartapi-app-ingress
  namespace: test
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cartapi-app-service
                port:
                  number: 80
