apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cartapi-ingress
spec:
  rules:
    - host: localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cartapi-service
                port:
                  number: 80
